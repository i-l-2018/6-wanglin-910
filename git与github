# 6-wanglin-910
##git与github
1.git是版本管理工具，版本控制系统（就是可以恢复到以前的版本的控制器），可认为是一个软件，可在不联网的情况下在本地编写自己的代码进行工作，git中的内容输入本地的数据;github是git的远程托管，是一个网站，不仅可以自己进行修改，别人也可以修改，适合相互交流和学习。
2.git可将提交过的代码push到远程仓库即github，而github可将自己仓库的代码fetch到本地。
##git的工作流程
将本地文件同步到远程仓库
1.	本地文件发生变动，进行自动查找变动
2.	将变动文件添加到stage（此过程由本地工作区迁移到暂存区）
3.	进行commit备注变动（此过程由暂存区迁移到本地仓库）
4.	Push（此过程由本地仓库迁移到远程仓库）
将远程仓库的变化同步到本地仓库
1．	Remote（fetch origin） （此过程由远程仓库到本地仓库）
2．	Merge（local merge）（此过程由本地仓库到本地工作区）

